# Workflow Bot Smoke Test

This temporary file exists only to trigger the repository's pull request bots and GitHub Actions workflows.

Expected checks include CI, PR test reporting, Danger warnings, and spelling lint.
